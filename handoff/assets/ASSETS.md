# ASSETS.md — inventory & to-shoot list

## Have (from this chat / project files)
- IMG_1547_2.jpg — real selfie, indoor. Usable for Strip frame 1 after duotone treatment.
- IMG_6075.PNG — polished headshot (note: appears AI-generated/enhanced — owner to
  confirm comfort using it as a "real photo"; if unsure, use only stylized/duotoned,
  or replace with a real shot).
- Profile.pdf, resume v5 PDF + LaTeX — resume.pdf source.

## To export (agent can produce during build)
- AutoDocxPdf mermaid PNGs — already in the repo (/mermaid_diagrams/*.png); pick
  "architecture & design" for Strip frame 3.
- AI_Bubble site preview — AI_Bubble.png in that repo, or re-screenshot live GitHub Pages.
- Schematic SVGs per Hall of Fame card (DESIGN-SPEC §3.5) — design task, Excalidraw
  style → clean SVG.

## Owner to capture (phone/SSH, 15 minutes total)
1. Terminal screenshot: TicketFlow booking flow logs (run locally, trigger a booking).
2. htop or agent dashboard on the Hostinger VPS (Hermes/OpenClaw) — redact hostnames/IPs.
3. Optional: one real candid (desk/Bengaluru) for warmth; campus shot for frame 7.
4. Re-export resume v5 PDF as `public/resume.pdf`.

## Rules
- Redact: client names, internal URLs, API keys, IPs, .env contents in every screenshot.
- Photos treated per DECISIONS D3 (default: indigo duotone).
- No third-party brand logos without license check — text wordmarks in the stack marquee.
